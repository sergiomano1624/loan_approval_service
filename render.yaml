services:
  - type: web
    name: loan-approval-service
    envVars:
      - key: DATABASE_URL
        value: postgres://loan_approval_user:pbnpeLikiGqrPcoHRLZMa6EJn7aNGWLy@dpg-clu0qnol5elc7387ostg-a.oregon-postgres.render.com/loan_approval
    buildCommand: pip install -r requirements.txt -r requirements.dev.txt
    startCommand: alembic upgrade head && uvicorn main:app --host 0.0.0.0 --port $PORT
    env:
      - key: FASTAPI_ENV
        value: production